import { MockTest } from "@/lib/core/types";
import {
  ALL_MOCK_MODULES,
  POLITY_MODULES,
  HISTORY_MODULES,
  ECONOMY_MODULES,
  GEOGRAPHY_MODULES,
} from "./subjects";

export const STATIC_MOCK_MODULES: MockTest[] = ALL_MOCK_MODULES;

export {
  POLITY_MODULES,
  HISTORY_MODULES,
  ECONOMY_MODULES,
  GEOGRAPHY_MODULES,
};
